<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Board">
    <!-- sql문이 작성되는 공간 -->
    <!-- parameterType에 "board"라고 되어있는 부분은 sql의 매개변수의 타입이 board라는 뜻
        제공되는 기본형 변수에 board라는 것은 없기 때문에 이것이 사용자 정의 매개변수라는 것을 알 수 있음-->
    <!-- useGeneratedKey - board_file_table에 데이터를 저장하기 위해서는 id가 필요한데 id는 java환경에서 넣어주는것이 아니라 mysql DB단에서
        생성되는것이기 때문에 java환경에서는 데이터가 insert되며 생성된 id를 알 수 없다. 때문에 id를 알기 위해서 제공되는 설정-->
    <!-- keyProperty - 받아온 데이터의 이름을 정해주는 것 -->
    <insert id="save" parameterType="board" useGeneratedKeys="true" keyProperty="id">
        insert into board_table(boardTitle, boardWriter, boardPass, boardContents, fileAttached)
        values(#{boardTitle}, #{boardWriter}, #{boardPass}, #{boardContents}, #{fileAttached})
    </insert>

    <select id="findAll" resultType="board">
        select id, boardTitle, boardWriter, boardHits, date_format(createdAt, "%Y-%m-%d") as createdAt
        from board_table
        order by id desc
    </select>

    <!-- boardHits의 카운트를 현재값에서 1추가 -->
    <update id="updateHits" parameterType="Long">
        update board_table
        set boardHits = boardHits+1 where id=#{id}
    </update>
    <!-- id값을 가지고 해당하는 글을 조회 -->
    <select id="findById" parameterType="Long" resultType="board">
        select id, boardTitle, boardWriter, boardPass, boardContents, boardHits, fileAttached,
               date_format(createdAt, "%Y-%m-%d") as createdAt
        from board_table
        where id=#{id}
    </select>

    <update id="update" parameterType="board">
        update board_table
        set boardTitle=#{boardTitle}, boardContents=#{boardContents}
        where id=#{id}
    </update>

    <delete id="delete" parameterType="Long">
        delete from board_table
        where id=#{id}
    </delete>

    <insert id="saveFile" parameterType="boardFile">
        insert into board_file_table(originalFileName, storedFileName, boardId)
        values (#{originalFileName}, #{storedFileName}, #{boardId})
    </insert>

    <select id="findFile" parameterType="Long" resultType="boardFile">
        select * from board_file_table where boardId = #{id}
    </select>

</mapper>